const require_rolldown_runtime = require("./rolldown-runtime-u92d-OFm.cjs");
const require_home_dir = require("./home-dir-DDyi_SB-.cjs");
require("./utils-CXqBhRFw.cjs");
const require_session_key = require("./session-key-BQFkCTNx.cjs");
const require_model_input = require("./model-input-DO-er-Kk.cjs");
const require_agent_scope = require("./agent-scope-Ce0XqMNr.cjs");
const require_agent_scope_config = require("./agent-scope-config-DpWhyljG.cjs");
const require_diagnostic_events = require("./diagnostic-events-BfVh8qZb.cjs");
const require_command_registration = require("./command-registration-COcka0py.cjs");
const require_retry = require("./retry-DXZi6qkk.cjs");
const require_sqlite_marker = require("./sqlite-marker-c45e72lc.cjs");
const require_errors = require("./errors-BqS4bzom.cjs");
const require_current_plugin_metadata_snapshot = require("./current-plugin-metadata-snapshot-C2Dl5h_D.cjs");
const require_message_channel = require("./message-channel-jMzaqV09.cjs");
const require_embedded_agent_helpers = require("./embedded-agent-helpers-DJEcJifp.cjs");
const require_selection = require("./selection-BpqUSi0C.cjs");
const require_hook_runner_global = require("./hook-runner-global-De_h3eqM.cjs");
const require_diagnostic = require("./diagnostic-Blh06VbF.cjs");
const require_defaults = require("./defaults-BplP0QgT.cjs");
const require_openai_routing = require("./openai-routing-B2l3ny9C.cjs");
const require_policy = require("./policy-DHgMAqLv.cjs");
const require_provider_request_config = require("./provider-request-config-BmGl8zwP.cjs");
const require_date_time = require("./date-time-zxjypawc.cjs");
const require_agent_tools = require("./agent-tools-C4N0fa5t.cjs");
const require_conversation_capability_profile = require("./conversation-capability-profile-Cn8o5WHy.cjs");
const require_text_transforms_runtime = require("./text-transforms.runtime-B0pRaBUe.cjs");
const require_provider_secret_egress = require("./provider-secret-egress-NB6SfEEF.cjs");
const require_agent_bundle_lsp_runtime = require("./agent-bundle-lsp-runtime-DtCWr2fk.cjs");
const require_provider_runtime = require("./provider-runtime-Blezec6-.cjs");
const require_store = require("./store-BgTrp0qP.cjs");
const require_agent_bundle_mcp_tools = require("./agent-bundle-mcp-tools-e1AmWJ1L.cjs");
const require_diagnostic_llm_content = require("./diagnostic-llm-content-DpdEBJOd.cjs");
const require_availability = require("./availability-BtRDBgBn.cjs");
const require_failover_error = require("./failover-error-voHYvp7k.cjs");
const require_gateway = require("./gateway-Dd-v0MLd.cjs");
const require_sessions = require("./sessions-Bcpn-MAP.cjs");
const require_logger = require("./logger-B-gij7u9.cjs");
const require_bootstrap_files = require("./bootstrap-files-DNCXmTWO.cjs");
const require_heartbeat_system_prompt = require("./heartbeat-system-prompt-BKoDGXAZ.cjs");
const require_system_prompt_report = require("./system-prompt-report-B1gQuzkf.cjs");
const require_attempt_prompt_helpers = require("./attempt.prompt-helpers-Dk0zUrmw.cjs");
const require_transcript_rewrite = require("./transcript-rewrite-CrjemdE4.cjs");
const require_session_write_lock = require("./session-write-lock-BTWJIoPj.cjs");
const require_session_manager = require("./session-manager-Bhv4hvYF.cjs");
const require_session_transcript_repair = require("./session-transcript-repair-vqlcO05-.cjs");
const require_shell_utils = require("./shell-utils-VLuMOGgy.cjs");
const require_model_auth_runtime_shared = require("./model-auth-runtime-shared-UOjMKX1E.cjs");
const require_model_auth = require("./model-auth-D9ZnqE0T.cjs");
const require_model_fallback = require("./model-fallback-MSKXoSVI.cjs");
const require_models_config = require("./models-config-kAzoM1Dq.cjs");
const require_agent_model_discovery = require("./agent-model-discovery-k4IOdehL.cjs");
const require_provider_stream = require("./provider-stream-DRQnPAya.cjs");
const require_openai_transport_stream = require("./openai-transport-stream-BqxWn1Ig.cjs");
const require_thinking_runtime = require("./thinking-runtime-CrpgBgYy.cjs");
const require_prepare_auth = require("./prepare-auth-jI6h10E_.cjs");
const require_tools = require("./tools-cldx6uki.cjs");
const require_effective_tool_policy = require("./effective-tool-policy-Cfe5Df2e.cjs");
const require_extra_params = require("./extra-params-CBjN2etI.cjs");
const require_attempt_model_diagnostic_events = require("./attempt.model-diagnostic-events-D5B6PnSa.cjs");
const require_system_prompt_params = require("./system-prompt-params-Bvd3P1G7.cjs");
const require_provider_utils = require("./provider-utils-ivH7d8vm.cjs");
const require_agent_settings = require("./agent-settings-CmUtgA2I.cjs");
const require_os_summary = require("./os-summary-DapJiOfZ.cjs");
const require_context_window_guard = require("./context-window-guard-1fyXlp_c.cjs");
const require_sandbox = require("./sandbox-CjshBxRn.cjs");
const require_utils$1 = require("./utils-CngUx0zS.cjs");
const require_machine_name = require("./machine-name-DYpOVD5W.cjs");
const require_docs_path = require("./docs-path-BiZHk9Qh.cjs");
const require_active_node_context = require("./active-node-context-FDYb96t_.cjs");
const require_sandbox_info = require("./sandbox-info-f5KIZ_13.cjs");
const require_model = require("./model-Don1-Go6.cjs");
const require_runtime_plugin = require("./runtime-plugin-9QTLb6UB.cjs");
const require_credential_scoped_model = require("./credential-scoped-model-D1XdNupI.cjs");
const require_resolve_auth = require("./resolve-auth-CVIGZ8Sc.cjs");
const require_build = require("./build-DNuxAERt.cjs");
const require_run_session_target = require("./run-session-target-Jsztz246.cjs");
const require_runtime_plugins = require("./runtime-plugins-Cv0iqeLD.cjs");
const require_compact_reasons = require("./compact-reasons-B7W78oRX.cjs");
let node_os = require("node:os");
node_os = require_rolldown_runtime.__toESM(node_os, 1);
let node_fs_promises = require("node:fs/promises");
node_fs_promises = require_rolldown_runtime.__toESM(node_fs_promises, 1);
//#region src/agents/embedded-agent-runner/compaction-session-agent.ts
async function prepareCompactionSessionAgent(params) {
	const authStorage = params.authStorage && typeof params.authStorage === "object" && "getApiKey" in params.authStorage && typeof params.authStorage.getApiKey === "function" ? params.authStorage : void 0;
	const transportApiKey = authStorage ? await require_attempt_model_diagnostic_events.resolveEmbeddedAgentApiKey({
		provider: params.effectiveModel.provider,
		resolvedApiKey: params.resolvedApiKey,
		authStorage
	}) : params.resolvedApiKey;
	params.session.agent.streamFn = require_attempt_model_diagnostic_events.resolveEmbeddedAgentStreamFn({
		currentStreamFn: require_attempt_model_diagnostic_events.resolveEmbeddedAgentBaseStreamFn({ session: params.session }),
		providerStreamFn: params.providerStreamFn,
		sessionId: params.sessionId,
		signal: params.signal,
		model: params.effectiveModel,
		resolvedApiKey: params.resolvedApiKey,
		transportAuthAvailable: Boolean(transportApiKey?.trim()),
		authProfileId: params.runtimePlan?.auth.forwardedAuthProfileId,
		authStorage: params.authStorage
	});
	const providerTextTransforms = require_provider_runtime.resolveProviderTextTransforms({
		provider: params.provider,
		config: params.config,
		workspaceDir: params.effectiveWorkspace
	});
	if (providerTextTransforms) params.session.agent.streamFn = require_text_transforms_runtime.wrapStreamFnTextTransforms({
		streamFn: params.session.agent.streamFn,
		input: providerTextTransforms.input,
		output: providerTextTransforms.output,
		transformSystemPrompt: false
	});
	const providerThinkingLevel = require_utils$1.mapThinkingLevelForProvider(params.thinkLevel);
	const preparedRuntimeExtraParams = params.runtimePlan?.transport.resolveExtraParams({
		thinkingLevel: providerThinkingLevel,
		agentId: params.sessionAgentId,
		workspaceDir: params.effectiveWorkspace,
		model: params.effectiveModel
	});
	return require_extra_params.applyExtraParamsToAgent(params.session.agent, params.config, params.provider, params.modelId, void 0, providerThinkingLevel, params.sessionAgentId, params.effectiveWorkspace, params.effectiveModel, params.agentDir, void 0, {
		...preparedRuntimeExtraParams ? { preparedExtraParams: preparedRuntimeExtraParams } : {},
		nativeWebSearchPolicyContext: {
			sessionKey: params.sessionKey,
			sandboxToolPolicy: params.sandboxToolPolicy,
			messageProvider: params.messageProvider,
			agentAccountId: params.agentAccountId,
			groupId: params.groupId,
			groupChannel: params.groupChannel,
			groupSpace: params.groupSpace,
			spawnedBy: params.spawnedBy,
			senderId: params.senderId,
			senderName: params.senderName,
			senderUsername: params.senderUsername,
			senderE164: params.senderE164
		}
	});
}
//#endregion
//#region src/agents/embedded-agent-runner/manual-compaction-boundary.ts
function replaceLatestCompactionBoundary(params) {
	return params.entries.map((entry) => {
		if (entry.type !== "compaction" || entry.id !== params.compactionEntryId) return entry;
		return {
			...entry,
			firstKeptEntryId: entry.id
		};
	});
}
function entryCreatesCompactionInputMessage(entry) {
	return entry.type === "message" || entry.type === "custom_message" || entry.type === "branch_summary";
}
function hasMessagesToSummarizeBeforeKeptTail(params) {
	const compactionIndex = params.branch.findIndex((entry) => entry.id === params.compaction.id);
	const firstKeptIndex = params.branch.findIndex((entry) => entry.id === params.compaction.firstKeptEntryId);
	if (compactionIndex <= 0 || firstKeptIndex < 0 || firstKeptIndex >= compactionIndex) return false;
	let boundaryStartIndex = 0;
	for (let i = compactionIndex - 1; i >= 0; i -= 1) {
		const entry = params.branch[i];
		if (entry?.type !== "compaction") continue;
		const previousFirstKeptIndex = params.branch.findIndex((candidate) => candidate.id === entry.firstKeptEntryId);
		boundaryStartIndex = previousFirstKeptIndex >= 0 ? previousFirstKeptIndex : i + 1;
		break;
	}
	return params.branch.slice(boundaryStartIndex, firstKeptIndex).some((entry) => entryCreatesCompactionInputMessage(entry));
}
/** Rewrite the latest manual compaction leaf so replay starts from its summary. */
async function hardenManualCompactionBoundary(params) {
	const state = await require_transcript_rewrite.readTranscriptFileState(params.sessionFile);
	const header = state.getHeader();
	if (!header) return {
		applied: false,
		messages: []
	};
	const leaf = state.getLeafEntry();
	if (leaf?.type !== "compaction") {
		const sessionContext = state.buildSessionContext();
		return {
			applied: false,
			leafId: state.getLeafId() ?? void 0,
			messages: sessionContext.messages
		};
	}
	const sessionContext = state.buildSessionContext();
	if (params.preserveRecentTail) return {
		applied: false,
		firstKeptEntryId: leaf.firstKeptEntryId,
		leafId: state.getLeafId() ?? void 0,
		messages: sessionContext.messages
	};
	if (leaf.firstKeptEntryId === leaf.id) return {
		applied: false,
		firstKeptEntryId: leaf.id,
		leafId: state.getLeafId() ?? void 0,
		messages: sessionContext.messages
	};
	if (!leaf.summary.trim() || !hasMessagesToSummarizeBeforeKeptTail({
		branch: state.getBranch(leaf.id),
		compaction: leaf
	})) return {
		applied: false,
		firstKeptEntryId: leaf.firstKeptEntryId,
		leafId: state.getLeafId() ?? void 0,
		messages: sessionContext.messages
	};
	const replacedEntries = replaceLatestCompactionBoundary({
		entries: state.getEntries(),
		compactionEntryId: leaf.id
	});
	const replacedState = new require_transcript_rewrite.TranscriptFileState({
		header,
		entries: replacedEntries
	});
	await require_transcript_rewrite.writeTranscriptFileAtomic(params.sessionFile, [header, ...replacedEntries]);
	const replacedSessionContext = replacedState.buildSessionContext();
	return {
		applied: true,
		firstKeptEntryId: leaf.id,
		leafId: replacedState.getLeafId() ?? void 0,
		messages: replacedSessionContext.messages
	};
}
//#endregion
//#region src/agents/embedded-agent-runner/compact.ts
/**
* Implements embedded-agent transcript compaction and runtime handoff.
*/
const compactionCheckpointStore = require_build.createFileBackedCompactionCheckpointStore();
function hasRealConversationContent(msg, messages, index) {
	return require_selection.isRealConversationMessage(msg, messages, index);
}
function createCompactionDiagId() {
	return `cmp-${Date.now().toString(36)}-${require_retry.generateSecureToken(4)}`;
}
function resolveCompactionProviderStream(params) {
	return require_provider_stream.registerProviderStreamForModel({
		model: params.effectiveModel,
		cfg: params.config,
		agentDir: params.agentDir,
		workspaceDir: params.effectiveWorkspace
	});
}
function normalizeObservedTokenCount(value) {
	return typeof value === "number" && Number.isFinite(value) && value > 0 ? Math.floor(value) : void 0;
}
function getMessageTextChars(msg) {
	const content = msg.content;
	if (typeof content === "string") return content.length;
	if (!Array.isArray(content)) return 0;
	let total = 0;
	for (const block of content) {
		if (!block || typeof block !== "object") continue;
		const text = block.text;
		if (typeof text === "string") total += text.length;
	}
	return total;
}
function resolveMessageToolLabel(msg) {
	const candidate = msg.toolName ?? msg.name ?? msg.tool;
	return typeof candidate === "string" && candidate.trim().length > 0 ? candidate : void 0;
}
function summarizeCompactionMessages(messages) {
	let historyTextChars = 0;
	let toolResultChars = 0;
	const contributors = [];
	let estTokens = 0;
	let tokenEstimationFailed = false;
	for (const msg of messages) {
		const role = typeof msg.role === "string" ? msg.role : "unknown";
		const chars = getMessageTextChars(msg);
		historyTextChars += chars;
		if (role === "toolResult") toolResultChars += chars;
		contributors.push({
			role,
			chars,
			tool: resolveMessageToolLabel(msg)
		});
		if (!tokenEstimationFailed) try {
			estTokens += require_session_manager.estimateTokens(msg);
		} catch {
			tokenEstimationFailed = true;
		}
	}
	return {
		messages: messages.length,
		historyTextChars,
		toolResultChars,
		estTokens: tokenEstimationFailed ? void 0 : estTokens,
		contributors: selectTopContributors(contributors)
	};
}
function selectTopContributors(contributors) {
	const selected = [];
	for (const contributor of contributors) {
		let insertAt = selected.length;
		for (let index = 0; index < selected.length; index += 1) {
			const selectedContributor = selected.at(index);
			if (selectedContributor && contributor.chars > selectedContributor.chars) {
				insertAt = index;
				break;
			}
		}
		if (insertAt < 3) {
			selected.splice(insertAt, 0, contributor);
			if (selected.length > 3) selected.pop();
		} else if (selected.length < 3) selected.push(contributor);
	}
	return selected;
}
function containsRealConversationMessages(messages) {
	return messages.some((message, index, allMessages) => hasRealConversationContent(message, allMessages, index));
}
function hasExplicitCompactionModel(params) {
	return Boolean(params.config?.agents?.defaults?.compaction?.model?.trim());
}
function resolveCompactionFallbacksOverride(params) {
	if (params.modelSelectionLocked) return [];
	return params.modelFallbacksOverride ?? require_agent_scope.resolveRunModelFallbacksOverride({
		cfg: params.config,
		sessionKey: params.sessionKey
	});
}
function hasCompactionModelFallbackCandidates(params) {
	const fallbacksOverride = resolveCompactionFallbacksOverride(params);
	const defaultFallbacks = require_model_input.resolveAgentModelFallbackValues(params.config?.agents?.defaults?.model);
	return (fallbacksOverride ?? defaultFallbacks).length > 0;
}
function classifyCompactionFallbackResult(result, provider, model) {
	if (result.ok) return null;
	const reason = result.reason?.trim();
	if (!reason) return null;
	const failoverError = require_failover_error.coerceToFailoverError(Object.assign(new Error(result.failure?.rawError ?? reason), {
		status: result.failure?.status,
		code: result.failure?.code
	}), {
		provider,
		model
	});
	return failoverError ? { error: failoverError } : null;
}
function fallbackFailureToCompactionResult(err) {
	return {
		ok: false,
		compacted: false,
		reason: require_model_fallback.isFallbackSummaryError(err) ? err.message : require_errors.formatErrorMessage(err)
	};
}
/**
* Core compaction logic without lane queueing.
* Use this when already inside a session/global lane to avoid deadlocks.
*/
async function compactEmbeddedAgentSessionDirect(paramsInput) {
	const paramsBase = require_run_session_target.applyAgentRunSessionTargetIdentity(paramsInput);
	const lockedHarnessRuntime = require_openai_routing.normalizeOptionalAgentRuntimeId(paramsBase.agentHarnessId);
	if (paramsBase.modelSelectionLocked === true && lockedHarnessRuntime !== "@gabrielvfonseca/operator") return {
		ok: false,
		compacted: false,
		reason: lockedHarnessRuntime ? `Model selection is locked to native agent harness "${lockedHarnessRuntime}"; generic compaction is unavailable.` : "Model selection is locked but the persisted agent harness is unavailable.",
		failure: { reason: "model_selection_locked" }
	};
	const runSessionTarget = await require_run_session_target.resolveAgentRunSessionTarget(paramsBase);
	const params = {
		...paramsBase,
		agentId: paramsBase.agentId ?? runSessionTarget.agentId,
		sessionId: runSessionTarget.sessionId,
		sessionKey: paramsBase.sessionKey ?? runSessionTarget.sessionKey,
		sessionFile: runSessionTarget.sessionFile
	};
	if (hasExplicitCompactionModel(params) || !hasCompactionModelFallbackCandidates(params)) return await compactEmbeddedAgentSessionDirectOnce(params);
	const resolvedCompactionTarget = require_attempt_prompt_helpers.resolveEmbeddedCompactionTarget({
		config: params.config,
		provider: params.provider,
		modelId: params.model,
		authProfileId: params.authProfileId,
		modelSelectionLocked: params.modelSelectionLocked,
		defaultProvider: require_defaults.DEFAULT_PROVIDER,
		defaultModel: require_defaults.DEFAULT_MODEL
	});
	const primaryProvider = resolvedCompactionTarget.provider ?? "openrouter";
	const primaryModel = resolvedCompactionTarget.model ?? "openrouter/auto";
	const requestedPrimaryProvider = params.provider?.trim() || "openrouter";
	const fallbacksOverride = resolveCompactionFallbacksOverride(params);
	const resolvedPrimaryCandidate = require_model_fallback.resolveModelCandidateChain({
		cfg: params.config,
		provider: primaryProvider,
		model: primaryModel,
		fallbacksOverride
	})[0];
	const fallbackAgentId = require_agent_scope.resolveSessionAgentIds({
		sessionKey: params.sandboxSessionKey ?? params.sessionKey,
		config: params.config,
		agentId: params.agentId
	}).sessionAgentId;
	const fallbackSessionKey = params.sandboxSessionKey ?? params.sessionKey ?? params.sessionId;
	try {
		return (await require_model_fallback.runWithModelFallback({
			cfg: params.config,
			provider: primaryProvider,
			model: primaryModel,
			runId: params.runId ?? params.sessionId,
			agentDir: params.agentDir,
			agentId: fallbackAgentId,
			sessionId: params.sessionId,
			sessionKey: fallbackSessionKey,
			abortSignal: params.abortSignal,
			prepareAgentHarnessRuntime: async ({ provider, model, agentHarnessRuntimeOverride }) => {
				await require_runtime_plugin.ensureSelectedAgentHarnessPlugin({
					config: params.config,
					provider,
					modelId: model,
					agentId: fallbackAgentId,
					sessionKey: fallbackSessionKey,
					agentHarnessRuntimeOverride,
					workspaceDir: params.workspaceDir
				});
			},
			fallbacksOverride,
			classifyResult: ({ result, provider, model }) => classifyCompactionFallbackResult(result, provider, model),
			run: async (provider, model) => {
				const isPrimaryCandidate = provider === resolvedPrimaryCandidate?.provider && model === resolvedPrimaryCandidate.model;
				const preservesPrimaryAuth = isPrimaryCandidate || provider === primaryProvider || provider === requestedPrimaryProvider;
				const authProfileId = preservesPrimaryAuth ? params.authProfileId : void 0;
				const candidateThinkLevel = require_thinking_runtime.resolveCandidateThinkingLevel({
					cfg: params.config,
					provider,
					modelId: model,
					level: params.thinkLevel,
					agentId: fallbackAgentId,
					sessionKey: fallbackSessionKey,
					agentRuntime: params.agentHarnessId
				});
				return await compactEmbeddedAgentSessionDirectOnce({
					...params,
					provider,
					model,
					authProfileId,
					authProfileIdSource: preservesPrimaryAuth ? params.authProfileIdSource : void 0,
					thinkLevel: candidateThinkLevel,
					runtimeAuthPlan: isPrimaryCandidate ? params.runtimeAuthPlan : void 0,
					runtimePlan: isPrimaryCandidate ? params.runtimePlan : void 0
				});
			}
		})).result;
	} catch (err) {
		return fallbackFailureToCompactionResult(err);
	}
}
async function compactEmbeddedAgentSessionDirectOnce(params) {
	const startedAt = Date.now();
	const diagId = params.diagId?.trim() || createCompactionDiagId();
	const trigger = params.trigger ?? "manual";
	const attempt = params.attempt ?? 1;
	const maxAttempts = params.maxAttempts ?? 1;
	const runId = params.runId ?? params.sessionId;
	const compactionModelCallTrace = require_diagnostic_events.freezeDiagnosticTraceContext(require_diagnostic_events.getActiveDiagnosticTraceContext() ?? require_diagnostic_events.createDiagnosticTraceContext());
	const diagnosticCompactionRunId = `${runId}:compaction:${diagId}`;
	let diagnosticModelCallSeq = 0;
	const resolvedWorkspace = require_home_dir.resolveUserPath(params.workspaceDir);
	require_runtime_plugins.ensureRuntimePluginsLoaded({
		config: params.config,
		workspaceDir: resolvedWorkspace,
		allowGatewaySubagentBinding: params.allowGatewaySubagentBinding
	});
	const earlyAgentIds = require_agent_scope.resolveSessionAgentIds({
		sessionKey: params.sessionKey,
		config: params.config,
		agentId: params.agentId
	});
	const agentDir = params.agentDir ?? require_agent_scope_config.resolveAgentDir(params.config ?? {}, earlyAgentIds.sessionAgentId);
	const runtimePolicySessionKey = params.sandboxSessionKey ?? params.sessionKey;
	const runtimePolicyAgentId = params.sandboxSessionKey && require_session_key.parseAgentSessionKey(params.sandboxSessionKey) ? void 0 : params.agentId;
	const policyCompactionTarget = require_attempt_prompt_helpers.resolveEmbeddedCompactionTarget({
		config: params.config,
		provider: params.provider,
		modelId: params.model,
		authProfileId: params.authProfileId,
		modelSelectionLocked: params.modelSelectionLocked,
		defaultProvider: require_defaults.DEFAULT_PROVIDER,
		defaultModel: require_defaults.DEFAULT_MODEL
	});
	const policyProvider = policyCompactionTarget.provider ?? "openrouter";
	const policyModelId = policyCompactionTarget.model ?? "openrouter/auto";
	const configuredHarnessPolicy = require_policy.resolveAgentHarnessPolicy({
		provider: policyProvider,
		modelId: policyModelId,
		config: params.config,
		agentId: runtimePolicyAgentId,
		sessionKey: runtimePolicySessionKey
	});
	const configuredHarnessRuntime = configuredHarnessPolicy.runtimeSource && configuredHarnessPolicy.runtimeSource !== "implicit" && !require_openai_routing.isDefaultAgentRuntimeId(configuredHarnessPolicy.runtime) ? configuredHarnessPolicy.runtime : void 0;
	const boundHarnessRuntime = require_openai_routing.normalizeOptionalAgentRuntimeId(params.agentHarnessId);
	const selectedHarnessRuntime = require_attempt_prompt_helpers.resolveCompactionHarnessRuntime({
		boundHarnessRuntime,
		preparedRuntimePlan: params.runtimePlan,
		configuredHarnessRuntime,
		provider: policyProvider,
		modelId: policyModelId
	});
	const selectedHarnessRuntimeOverride = boundHarnessRuntime ? void 0 : selectedHarnessRuntime;
	const resolvedCompactionTarget = require_attempt_prompt_helpers.resolveEmbeddedCompactionTarget({
		config: params.config,
		provider: params.provider,
		modelId: params.model,
		authProfileId: params.authProfileId,
		harnessRuntime: selectedHarnessRuntime,
		modelSelectionLocked: params.modelSelectionLocked,
		defaultProvider: require_defaults.DEFAULT_PROVIDER,
		defaultModel: require_defaults.DEFAULT_MODEL
	});
	const provider = resolvedCompactionTarget.provider ?? "openrouter";
	const runtimeProvider = resolvedCompactionTarget.runtimeProvider ?? provider;
	const contextConfigProvider = resolvedCompactionTarget.contextProvider ?? provider;
	const modelId = resolvedCompactionTarget.model ?? "openrouter/auto";
	const { plan: reusableRuntimeAuthPlan, authProfileId, modelAuth: initialModelAuth } = require_credential_scoped_model.resolveReusableRuntimeModelAuth({
		plan: params.runtimeAuthPlan ?? params.runtimePlan?.auth,
		provider,
		modelId,
		authProfileId: resolvedCompactionTarget.authProfileId
	});
	await require_runtime_plugin.ensureSelectedAgentHarnessPlugin({
		config: params.config,
		provider,
		modelId,
		agentId: runtimePolicyAgentId,
		sessionKey: runtimePolicySessionKey,
		agentHarnessId: boundHarnessRuntime,
		agentHarnessRuntimeOverride: selectedHarnessRuntimeOverride,
		workspaceDir: resolvedWorkspace
	});
	let thinkLevel = params.thinkLevel ?? "off";
	const attemptedThinking = /* @__PURE__ */ new Set();
	const fail = (reason, err) => {
		const failureReason = require_compact_reasons.classifyCompactionReason(reason);
		const failure = err ? require_failover_error.describeFailoverError(err) : void 0;
		const detail = failureReason === "unknown" ? require_compact_reasons.formatUnknownCompactionReasonDetail(reason) : void 0;
		const detailSuffix = detail ? ` detail=${detail}` : "";
		require_logger.log.warn(`[compaction-diag] end runId=${runId} sessionKey=${params.sessionKey ?? params.sessionId} diagId=${diagId} trigger=${trigger} provider=${provider}/${modelId} attempt=${attempt} maxAttempts=${maxAttempts} outcome=failed reason=${failureReason}${detailSuffix} durationMs=${Date.now() - startedAt}`);
		return {
			ok: false,
			compacted: false,
			reason,
			failure: failure ? {
				reason: failure.reason,
				status: failure.status,
				code: failure.code,
				rawError: failure.rawError ?? failure.message
			} : void 0
		};
	};
	await require_models_config.ensureOperatorModelsJson(params.config, agentDir, { workspaceDir: resolvedWorkspace });
	const { model, error, authStorage, modelRegistry } = await require_model.resolveModelAsync(runtimeProvider, modelId, agentDir, params.config, initialModelAuth);
	if (!model) return fail(error ?? `Unknown model: ${runtimeProvider}/${modelId}`);
	const runtimeAuthProfileStore = require_openai_routing.isOpenAIProvider(provider) ? require_store.ensureAuthProfileStore(agentDir, {
		externalCliProviderIds: ["openai"],
		allowKeychainPrompt: false
	}) : require_store.ensureAuthProfileStoreWithoutExternalProfiles(agentDir, { allowKeychainPrompt: false });
	const providerUsesProfileScopedModelMetadata = require_credential_scoped_model.providerUsesCredentialScopedModelMetadata({
		provider: runtimeProvider,
		modelId,
		config: params.config,
		agentDir,
		workspaceDir: resolvedWorkspace
	});
	const selectHarnessForPreparedAttempts = (attempts) => require_selection.selectAgentHarnessForPreparedModelProviders({
		provider,
		modelId,
		modelProviders: attempts.map((authAttempt) => require_build.buildCompactionHarnessModelProvider({
			model,
			plan: authAttempt.plan,
			attempt: authAttempt
		})),
		config: params.config,
		agentId: runtimePolicyAgentId,
		sessionKey: runtimePolicySessionKey,
		agentHarnessId: boundHarnessRuntime,
		agentHarnessRuntimeOverride: selectedHarnessRuntimeOverride
	});
	const initialHarness = reusableRuntimeAuthPlan ? void 0 : require_selection.selectAgentHarness({
		provider,
		modelId,
		modelProvider: require_build.buildCompactionHarnessModelProvider({ model }),
		config: params.config,
		agentId: runtimePolicyAgentId,
		sessionKey: runtimePolicySessionKey,
		agentHarnessId: boundHarnessRuntime,
		agentHarnessRuntimeOverride: selectedHarnessRuntimeOverride
	});
	const prepareRuntimeAuth = (harness) => require_prepare_auth.prepareAgentRuntimeAuth({
		provider,
		modelId,
		modelApi: model.api,
		modelBaseUrl: model.baseUrl,
		config: params.config,
		env: process.env,
		agentDir,
		workspaceDir: resolvedWorkspace,
		authProfileStore: runtimeAuthProfileStore,
		sessionAuthProfileId: authProfileId,
		sessionAuthProfileSource: params.authProfileIdSource,
		harnessId: harness.id,
		harnessRuntime: harness.id,
		harnessAuthBootstrap: harness.authBootstrap
	});
	let runtimeAuthPreparation = reusableRuntimeAuthPlan ? {
		plan: reusableRuntimeAuthPlan,
		attempts: [{
			kind: "implicit",
			plan: reusableRuntimeAuthPlan
		}]
	} : prepareRuntimeAuth(initialHarness);
	let selectedPreparedHarness = selectHarnessForPreparedAttempts(runtimeAuthPreparation.attempts);
	if (!reusableRuntimeAuthPlan && selectedPreparedHarness.id !== initialHarness?.id) {
		runtimeAuthPreparation = prepareRuntimeAuth(selectedPreparedHarness);
		const confirmedHarness = selectHarnessForPreparedAttempts(runtimeAuthPreparation.attempts);
		if (confirmedHarness.id !== selectedPreparedHarness.id) throw new Error(`Prepared compaction auth routes did not converge on one agent harness for ${provider}/${modelId}.`);
		selectedPreparedHarness = confirmedHarness;
	}
	const preparedHarnessRuntime = selectedPreparedHarness.id;
	const resolvePreparedModel = ({ config, authProfileId: profileId, authProfileMode: resolvedAuthProfileMode }) => require_model.resolveModelAsync(runtimeProvider, modelId, agentDir, config, {
		authStorage,
		modelRegistry,
		skipAgentDiscovery: true,
		allowBundledStaticCatalogFallback: true,
		preferBundledStaticCatalogTransport: true,
		workspaceDir: resolvedWorkspace,
		authProfileId: profileId,
		authProfileMode: resolvedAuthProfileMode
	});
	const materializeAuthAttemptModel = async (materializeParams) => await require_credential_scoped_model.materializePreparedRuntimeModel({
		plan: materializeParams.plan,
		provider,
		modelId,
		config: params.config,
		model: materializeParams.model,
		forceResolve: materializeParams.forceResolve,
		resolveModel: resolvePreparedModel
	}) ?? materializeParams.model;
	const resolveRuntimeAuthAttempt = () => require_resolve_auth.resolvePreparedRuntimeAuthAttempts({
		attempts: runtimeAuthPreparation.attempts,
		store: runtimeAuthProfileStore,
		modelId,
		model,
		materializeModel: materializeAuthAttemptModel,
		forceCredentialScopedDirectModelResolve: providerUsesProfileScopedModelMetadata,
		resolveAuth: async ({ attempt: preparedAttempt, model: attemptModel }) => await require_resolve_auth.resolvePreparedRuntimeModelAuth({
			plan: preparedAttempt.plan,
			model: attemptModel,
			cfg: params.config,
			store: runtimeAuthProfileStore,
			agentDir,
			workspaceDir: resolvedWorkspace,
			...preparedAttempt.allowAuthProfileFallback !== void 0 ? { allowAuthProfileFallback: preparedAttempt.allowAuthProfileFallback } : {},
			secretSentinels: true
		}),
		errorMessage: `Prepared compaction auth attempts could not be resolved for ${provider}/${modelId}.`
	});
	let resolvedAuthAttempt;
	try {
		resolvedAuthAttempt = await resolveRuntimeAuthAttempt();
	} catch (err) {
		return fail(require_errors.formatErrorMessage(err), err);
	}
	let runtimeModel = resolvedAuthAttempt.model;
	const apiKeyInfo = resolvedAuthAttempt.auth;
	const resolvedRuntimeAuthPlan = resolvedAuthAttempt.plan;
	let hasRuntimeAuthExchange = false;
	try {
		if (!apiKeyInfo.apiKey) {
			if (apiKeyInfo.mode !== "aws-sdk") throw new require_model_auth_runtime_shared.MissingProviderAuthError(runtimeModel.provider, apiKeyInfo);
		} else {
			const preparedAuth = require_provider_secret_egress.protectPreparedProviderRuntimeAuth({
				provider: runtimeModel.provider,
				preparedAuth: await require_provider_runtime.prepareProviderRuntimeAuth({
					provider: runtimeModel.provider,
					config: params.config,
					workspaceDir: resolvedWorkspace,
					env: process.env,
					context: {
						config: params.config,
						agentDir,
						workspaceDir: resolvedWorkspace,
						env: process.env,
						provider: runtimeModel.provider,
						modelId,
						model: runtimeModel,
						apiKey: require_provider_secret_egress.unwrapSecretSentinelsForProviderEgress(apiKeyInfo.apiKey, "provider runtime auth exchange"),
						authMode: apiKeyInfo.mode,
						profileId: apiKeyInfo.profileId
					}
				})
			});
			runtimeModel = require_provider_request_config.applyPreparedRuntimeAuthToModel(runtimeModel, preparedAuth);
			const runtimeApiKey = preparedAuth?.apiKey ?? apiKeyInfo.apiKey;
			hasRuntimeAuthExchange = Boolean(preparedAuth?.apiKey);
			if (!runtimeApiKey) throw new Error(`Provider "${runtimeModel.provider}" runtime auth returned no apiKey.`);
			authStorage.setRuntimeApiKey(runtimeModel.provider, runtimeApiKey);
		}
	} catch (err) {
		return fail(require_errors.formatErrorMessage(err), err);
	}
	await node_fs_promises.default.mkdir(resolvedWorkspace, { recursive: true });
	const sandboxSessionKey = params.sandboxSessionKey?.trim() || params.sessionKey?.trim() || params.sessionId;
	const sandbox = await require_sandbox.resolveSandboxContext({
		config: params.config,
		execOverrides: params.execOverrides,
		sessionKey: sandboxSessionKey,
		workspaceDir: resolvedWorkspace
	});
	const effectiveWorkspace = sandbox?.enabled ? sandbox.workspaceAccess === "rw" ? resolvedWorkspace : sandbox.workspaceDir : resolvedWorkspace;
	const requestedCwd = params.cwd ? require_home_dir.resolveUserPath(params.cwd) : void 0;
	if (sandbox?.enabled && requestedCwd && requestedCwd !== resolvedWorkspace) throw new Error("cwd override is not supported for sandboxed embedded compaction runs; omit cwd or use the agent workspace as cwd");
	const effectiveCwd = sandbox?.enabled ? effectiveWorkspace : requestedCwd ?? effectiveWorkspace;
	await node_fs_promises.default.mkdir(effectiveWorkspace, { recursive: true });
	const isSqliteSessionTranscript = Boolean(require_sqlite_marker.parseSqliteSessionFileMarker(params.sessionFile));
	if (!isSqliteSessionTranscript) await require_embedded_agent_helpers.ensureSessionHeader({
		sessionFile: params.sessionFile,
		sessionId: params.sessionId,
		cwd: effectiveCwd
	});
	const { sessionAgentId: effectiveSkillAgentId } = earlyAgentIds;
	let compactionSessionManager = null;
	let checkpointSnapshot = null;
	let checkpointSnapshotRetained = false;
	try {
		const sessionLabel = params.sessionKey ?? params.sessionId;
		const resolvedMessageProvider = params.messageChannel ?? params.messageProvider;
		const { contextFiles } = require_bootstrap_files.resolveContextInjectionMode(params.config, effectiveSkillAgentId) === "never" ? { contextFiles: [] } : await require_bootstrap_files.resolveBootstrapContextForRun({
			workspaceDir: effectiveWorkspace,
			config: params.config,
			sessionKey: params.sessionKey,
			sessionId: params.sessionId,
			agentId: effectiveSkillAgentId,
			warn: require_bootstrap_files.makeBootstrapWarn({
				sessionLabel,
				warn: (message) => require_logger.log.warn(message)
			})
		});
		const runtimeModelWithContext = runtimeModel;
		const resolvedContextTokenBudget = require_utils$1.normalizeContextTokenBudget(require_context_window_guard.resolveContextWindowInfo({
			cfg: params.config,
			provider: contextConfigProvider,
			modelId,
			modelContextTokens: require_build.readAgentModelContextTokens(runtimeModel),
			modelContextWindow: runtimeModelWithContext.contextWindow,
			defaultTokens: 2e5
		}).tokens) ?? 2e5;
		const requestedContextTokenBudget = require_utils$1.normalizeContextTokenBudget(params.contextTokenBudget) ?? require_utils$1.normalizeContextTokenBudget(params.tokenBudget);
		const contextTokenBudget = Math.min(requestedContextTokenBudget ?? resolvedContextTokenBudget, resolvedContextTokenBudget);
		const effectiveModel = require_model_auth.applyAuthHeaderOverride(require_model_auth.applyLocalNoAuthHeaderOverride(contextTokenBudget < (runtimeModelWithContext.contextWindow ?? Infinity) ? {
			...runtimeModelWithContext,
			contextWindow: contextTokenBudget
		} : runtimeModelWithContext, apiKeyInfo), hasRuntimeAuthExchange ? null : apiKeyInfo, params.config);
		const reuseFullRuntimePlan = params.runtimePlan?.auth === resolvedRuntimeAuthPlan;
		const preparedRuntimePlan = (reuseFullRuntimePlan ? params.runtimePlan : void 0) ?? require_build.buildAgentRuntimePlan({
			provider,
			modelId,
			model: effectiveModel,
			modelApi: effectiveModel.api,
			harnessId: preparedHarnessRuntime,
			harnessRuntime: preparedHarnessRuntime,
			authProfileMode: resolvedRuntimeAuthPlan.selectedAuthMode,
			sessionAuthProfileId: resolvedRuntimeAuthPlan.forwardedAuthProfileId,
			sessionAuthProfileSource: resolvedRuntimeAuthPlan.forwardedAuthProfileSource,
			sessionAuthProfileCandidateIds: resolvedRuntimeAuthPlan.forwardedAuthProfileCandidateIds,
			modelRoute: resolvedRuntimeAuthPlan.modelRoute,
			config: params.config,
			workspaceDir: effectiveWorkspace,
			agentDir,
			agentId: effectiveSkillAgentId,
			thinkingLevel: require_utils$1.mapThinkingLevelForProvider(thinkLevel)
		});
		const runtimePlan = reuseFullRuntimePlan ? preparedRuntimePlan : {
			...preparedRuntimePlan,
			auth: resolvedRuntimeAuthPlan
		};
		const runAbortController = new AbortController();
		const spawnWorkspaceDir = effectiveCwd !== effectiveWorkspace ? resolvedWorkspace : require_system_prompt_report.resolveAttemptSpawnWorkspaceDir({
			sandbox,
			resolvedWorkspace
		});
		const runtimeCapabilityProfile = require_conversation_capability_profile.resolveConversationCapabilityProfile({
			config: params.config,
			sessionKey: sandboxSessionKey,
			runSessionKey: params.sessionKey && params.sessionKey !== sandboxSessionKey ? params.sessionKey : void 0,
			sessionId: params.sessionId,
			runId: params.runId,
			agentDir,
			agentAccountId: params.agentAccountId,
			messageProvider: resolvedMessageProvider,
			chatType: params.chatType,
			groupId: params.groupId,
			groupChannel: params.groupChannel,
			groupSpace: params.groupSpace,
			spawnedBy: params.spawnedBy,
			senderId: params.senderId,
			senderName: params.senderName,
			senderUsername: params.senderUsername,
			senderE164: params.senderE164,
			senderIsOwner: params.senderIsOwner,
			modelProvider: effectiveModel.provider,
			modelId,
			modelApi: effectiveModel.api,
			modelContextWindowTokens: contextTokenBudget,
			workspaceDir: effectiveWorkspace,
			cwd: effectiveCwd,
			spawnWorkspaceDir,
			sandboxToolPolicy: sandbox?.tools
		});
		const toolsEnabled = require_openai_transport_stream.supportsModelTools(effectiveModel);
		const toolsRaw = toolsEnabled ? require_agent_tools.createOperatorCodingTools({
			exec: {
				...params.execOverrides,
				config: params.config,
				elevated: params.bashElevated
			},
			sandbox,
			messageProvider: resolvedMessageProvider,
			clientCaps: params.clientCaps,
			chatType: params.chatType,
			agentAccountId: params.agentAccountId,
			sessionKey: sandboxSessionKey,
			runSessionKey: params.sessionKey && params.sessionKey !== sandboxSessionKey ? params.sessionKey : void 0,
			sessionId: params.sessionId,
			runId: params.runId,
			oneShotCliRun: params.oneShotCliRun,
			groupId: params.groupId,
			groupChannel: params.groupChannel,
			groupSpace: params.groupSpace,
			spawnedBy: params.spawnedBy,
			senderId: params.senderId,
			senderName: params.senderName,
			senderUsername: params.senderUsername,
			senderE164: params.senderE164,
			allowGatewaySubagentBinding: params.allowGatewaySubagentBinding,
			agentDir,
			cwd: effectiveCwd,
			workspaceDir: effectiveWorkspace,
			spawnWorkspaceDir,
			config: params.config,
			abortSignal: runAbortController.signal,
			sourceReplyDeliveryMode: params.sourceReplyDeliveryMode,
			modelProvider: effectiveModel.provider,
			modelId,
			modelHasVision: effectiveModel.input?.includes("image") ?? false,
			modelCompat: require_agent_model_discovery.extractModelCompat(effectiveModel),
			modelApi: effectiveModel.api,
			modelContextWindowTokens: contextTokenBudget,
			conversationCapabilityProfile: runtimeCapabilityProfile,
			modelAuthMode: require_model_auth.resolveModelAuthMode(effectiveModel.provider, params.config, void 0, { workspaceDir: effectiveWorkspace })
		}) : [];
		const runtimePlanModelContext = {
			workspaceDir: effectiveWorkspace,
			modelApi: effectiveModel.api,
			model: effectiveModel
		};
		const normalizableToolProjection = require_tools.filterProviderNormalizableTools(toolsEnabled ? toolsRaw : []);
		require_selection.logRuntimeToolSchemaQuarantine({
			diagnostics: normalizableToolProjection.diagnostics,
			tools: toolsEnabled ? toolsRaw : [],
			runId,
			agentId: effectiveSkillAgentId,
			sessionKey: params.sessionKey,
			sessionId: params.sessionId
		});
		const tools = runtimePlan.tools.normalize([...normalizableToolProjection.tools], runtimePlanModelContext);
		const bundleMcpRuntime = toolsEnabled ? await require_agent_bundle_mcp_tools.createBundleMcpToolRuntime({
			workspaceDir: effectiveWorkspace,
			cfg: params.config,
			reservedToolNames: tools.map((tool) => tool.name)
		}) : void 0;
		const bundleLspRuntime = toolsEnabled ? await require_agent_bundle_lsp_runtime.createBundleLspToolRuntime({
			workspaceDir: effectiveWorkspace,
			cfg: params.config,
			reservedToolNames: [...tools.map((tool) => tool.name), ...bundleMcpRuntime?.tools.map((tool) => tool.name) ?? []]
		}) : void 0;
		const filteredBundledTools = require_effective_tool_policy.applyFinalEffectiveToolPolicy({
			bundledTools: [...bundleMcpRuntime?.tools ?? [], ...bundleLspRuntime?.tools ?? []],
			config: params.config,
			conversationCapabilityProfile: runtimeCapabilityProfile,
			warn: (message) => require_logger.log.warn(message)
		});
		const normalizableBundledToolProjection = require_tools.filterProviderNormalizableTools(filteredBundledTools);
		if (normalizableBundledToolProjection.diagnostics.length > 0) require_selection.logRuntimeToolSchemaQuarantine({
			diagnostics: normalizableBundledToolProjection.diagnostics,
			tools: filteredBundledTools,
			runId,
			agentId: effectiveSkillAgentId,
			sessionKey: params.sessionKey,
			sessionId: params.sessionId
		});
		const normalizedBundledTools = filteredBundledTools.length > 0 ? runtimePlan.tools.normalize([...normalizableBundledToolProjection.tools], runtimePlanModelContext) : filteredBundledTools;
		const projectedEffectiveTools = [...tools, ...normalizedBundledTools];
		const toolSchemaProjection = require_tools.filterRuntimeCompatibleTools(projectedEffectiveTools);
		require_selection.logRuntimeToolSchemaQuarantine({
			diagnostics: toolSchemaProjection.diagnostics,
			tools: projectedEffectiveTools,
			runId,
			agentId: effectiveSkillAgentId,
			sessionKey: params.sessionKey,
			sessionId: params.sessionId
		});
		const effectiveTools = [...toolSchemaProjection.tools];
		const allowedToolNames = require_selection.collectAllowedToolNames({ tools: effectiveTools });
		runtimePlan.tools.logDiagnostics(effectiveTools, runtimePlanModelContext);
		const machineName = await require_machine_name.getMachineDisplayName();
		const runtimeChannel = require_message_channel.normalizeMessageChannel(params.messageChannel ?? params.messageProvider);
		const runtimeCapabilities = require_system_prompt_report.collectRuntimeChannelCapabilities({
			cfg: params.config,
			channel: runtimeChannel,
			accountId: params.agentAccountId
		});
		const reactionGuidance = runtimeChannel && params.config ? require_gateway.resolveChannelReactionGuidance({
			cfg: params.config,
			channel: runtimeChannel,
			accountId: params.agentAccountId
		}) : void 0;
		const { defaultAgentId, sessionAgentId } = require_agent_scope.resolveSessionAgentIds({
			sessionKey: params.sessionKey,
			config: params.config,
			agentId: params.agentId
		});
		const channelActions = runtimeChannel ? require_gateway.listChannelSupportedActions(require_selection.buildEmbeddedMessageActionDiscoveryInput({
			cfg: params.config,
			channel: runtimeChannel,
			currentChannelId: params.currentChannelId,
			currentThreadTs: params.currentThreadTs,
			currentMessageId: params.currentMessageId,
			accountId: params.agentAccountId,
			sessionKey: params.sessionKey,
			sessionId: params.sessionId,
			agentId: sessionAgentId,
			senderId: params.senderId
		})) : void 0;
		const messageToolHints = runtimeChannel ? require_gateway.resolveChannelMessageToolHints({
			cfg: params.config,
			channel: runtimeChannel,
			accountId: params.agentAccountId
		}) : void 0;
		const runtimeInfo = {
			agentId: sessionAgentId,
			sessionKey: params.sessionKey,
			host: machineName,
			os: require_os_summary.resolveRuntimeOsLabel(),
			arch: node_os.default.arch(),
			node: process.version,
			model: `${provider}/${modelId}`,
			shell: require_shell_utils.detectRuntimeShell(),
			channel: runtimeChannel,
			chatType: params.chatType,
			capabilities: runtimeCapabilities,
			channelActions,
			activeProcessSessions: require_attempt_prompt_helpers.listActiveProcessSessionReferences({ scopeKey: require_agent_tools.resolveProcessToolScopeKey({
				sessionKey: sandboxSessionKey,
				agentId: sessionAgentId
			}) }),
			activeNode: require_active_node_context.formatActiveNodeContextLabel(require_active_node_context.getActiveNodeContext())
		};
		const sandboxInfoExecPolicy = require_sandbox_info.resolveEmbeddedSandboxInfoExecPolicy({
			config: params.config,
			agentId: sessionAgentId,
			sessionKey: params.sessionKey,
			sandboxAvailable: sandbox?.enabled === true,
			execOverrides: params.execOverrides
		});
		const sandboxInfo = require_sandbox_info.buildEmbeddedSandboxInfo(sandbox, params.bashElevated, sandboxInfoExecPolicy);
		const reasoningTagHint = require_provider_utils.isReasoningTagProvider(provider, {
			config: params.config,
			workspaceDir: effectiveWorkspace,
			env: process.env,
			modelId,
			modelApi: effectiveModel.api,
			model: effectiveModel
		});
		const userTimezone = require_date_time.resolveUserTimezone(params.config?.agents?.defaults?.userTimezone);
		const userTimeFormat = require_date_time.resolveUserTimeFormat(params.config?.agents?.defaults?.timeFormat);
		const userTime = require_date_time.formatUserTime(/* @__PURE__ */ new Date(), userTimezone, userTimeFormat);
		const promptSurface = require_system_prompt_params.resolveAgentPromptSurfaceForSessionKey(params.sessionKey);
		const promptMode = require_session_key.isSubagentSessionKey(params.sessionKey) || require_session_key.isCronSessionKey(params.sessionKey) ? "minimal" : "full";
		const nativeCommandGuidanceLines = require_command_registration.listRegisteredPluginAgentPromptGuidance({ surface: promptSurface });
		const openClawReferences = await require_docs_path.resolveOperatorReferencePaths({
			workspaceDir: effectiveWorkspace,
			argv1: process.argv[1],
			cwd: effectiveCwd,
			moduleUrl: require("url").pathToFileURL(__filename).href
		});
		const promptContributionContext = {
			config: params.config,
			agentDir,
			workspaceDir: effectiveWorkspace,
			provider,
			modelId,
			promptMode,
			runtimeChannel,
			runtimeCapabilities,
			agentId: sessionAgentId
		};
		const promptContribution = runtimePlan.prompt.resolveSystemPromptContribution(promptContributionContext);
		const buildSystemPromptText = (defaultThinkLevel) => {
			const builtSystemPrompt = require_selection.buildEmbeddedSystemPrompt({
				config: params.config,
				agentId: sessionAgentId,
				workspaceDir: effectiveWorkspace,
				defaultThinkLevel,
				reasoningLevel: params.reasoningLevel ?? "off",
				extraSystemPrompt: params.extraSystemPrompt,
				ownerNumbers: params.ownerNumbers,
				reasoningTagHint,
				heartbeatPrompt: require_heartbeat_system_prompt.resolveHeartbeatPromptForSystemPrompt({
					config: params.config,
					agentId: sessionAgentId,
					defaultAgentId
				}),
				skillsPrompt: params.skillsPrompt,
				docsPath: openClawReferences.docsPath ?? void 0,
				sourcePath: openClawReferences.sourcePath ?? void 0,
				promptMode,
				promptSurface,
				sourceReplyDeliveryMode: params.sourceReplyDeliveryMode,
				acpEnabled: require_availability.isAcpRuntimeSpawnAvailable({
					config: params.config,
					sandboxed: sandboxInfo?.enabled === true
				}),
				runtimeInfo,
				reactionGuidance,
				messageToolHints,
				sandboxInfo,
				tools: effectiveTools,
				userTimezone,
				userTime,
				userTimeFormat,
				contextFiles,
				promptContribution,
				nativeCommandGuidanceLines
			});
			return require_provider_runtime.transformProviderSystemPrompt({
				provider,
				config: params.config,
				workspaceDir: effectiveWorkspace,
				context: {
					config: params.config,
					agentDir,
					workspaceDir: effectiveWorkspace,
					provider,
					modelId,
					promptMode,
					runtimeChannel,
					runtimeCapabilities,
					agentId: sessionAgentId,
					systemPrompt: builtSystemPrompt
				}
			});
		};
		const compactionTimeoutMs = require_diagnostic.resolveCompactionTimeoutMs(params.config);
		const sessionLock = await require_session_write_lock.acquireSessionWriteLock({
			sessionFile: params.sessionFile,
			...require_session_write_lock.resolveSessionWriteLockOptions(params.config, { maxHoldMsFallback: require_session_write_lock.resolveSessionLockMaxHoldFromTimeout({ timeoutMs: compactionTimeoutMs }) })
		});
		try {
			if (!isSqliteSessionTranscript) {
				await require_session_manager.repairSessionFileIfNeeded({
					sessionFile: params.sessionFile,
					debug: (message) => require_logger.log.debug(message),
					warn: (message) => require_logger.log.warn(message)
				});
				await require_selection.prewarmSessionFile(params.sessionFile);
			}
			const transcriptPolicy = runtimePlan.transcript.resolvePolicy(runtimePlanModelContext);
			const sessionManager = require_selection.guardSessionManager(require_session_manager.SessionManager.open(params.sessionFile), {
				agentId: sessionAgentId,
				sessionKey: params.sessionKey,
				config: params.config,
				contextWindowTokens: contextTokenBudget,
				allowSyntheticToolResults: transcriptPolicy.allowSyntheticToolResults,
				missingToolResultText: effectiveModel.api === "openai-responses" || effectiveModel.api === "azure-openai-responses" || effectiveModel.api === "openai-chatgpt-responses" ? "aborted" : void 0,
				allowedToolNames
			});
			checkpointSnapshot = await compactionCheckpointStore.captureSnapshot({
				sessionManager,
				sessionFile: params.sessionFile
			});
			compactionSessionManager = sessionManager;
			require_selection.trackSessionManagerAccess(params.sessionFile);
			const settingsManager = require_selection.createPreparedEmbeddedAgentSettingsManager({
				cwd: effectiveCwd,
				agentDir,
				cfg: params.config,
				pluginMetadataSnapshot: require_current_plugin_metadata_snapshot.getCurrentPluginMetadataSnapshot({
					config: params.config,
					env: process.env,
					workspaceDir: effectiveWorkspace
				}),
				contextTokenBudget
			});
			const resourceLoader = require_selection.createEmbeddedAgentResourceLoader({
				cwd: effectiveCwd,
				agentDir,
				settingsManager,
				extensionFactories: require_selection.buildEmbeddedExtensionFactories({
					cfg: params.config,
					sessionManager,
					provider,
					modelId,
					model: effectiveModel
				})
			});
			await resourceLoader.reload();
			require_agent_settings.applyAgentCompactionSettingsFromConfig({
				settingsManager,
				cfg: params.config,
				contextTokenBudget
			});
			require_agent_settings.applyAgentAutoCompactionGuard({
				settingsManager,
				silentOverflowProneProvider: require_agent_settings.isSilentOverflowProneModel({
					provider,
					modelId,
					baseUrl: effectiveModel.baseUrl ?? void 0
				})
			});
			const { customTools } = require_selection.splitSdkTools({
				tools: effectiveTools,
				sandboxEnabled: Boolean(sandbox?.enabled),
				toolHookContext: {
					agentId: sessionAgentId,
					config: params.config,
					cwd: effectiveCwd,
					sessionKey: sandboxSessionKey,
					sessionId: params.sessionId,
					runId: params.runId,
					channelId: params.currentChannelId
				}
			});
			const sessionToolAllowlist = require_selection.toSessionToolAllowlist(require_selection.collectRegisteredToolNames(customTools));
			const providerStreamFn = resolveCompactionProviderStream({
				effectiveModel,
				config: params.config,
				agentDir,
				effectiveWorkspace
			});
			while (true) {
				attemptedThinking.add(thinkLevel);
				const systemPromptText = buildSystemPromptText(thinkLevel);
				let session;
				try {
					session = (await require_sessions.createAgentSession({
						cwd: effectiveCwd,
						agentDir,
						authStorage,
						modelRegistry,
						model: effectiveModel,
						thinkingLevel: require_utils$1.mapThinkingLevel(thinkLevel),
						tools: sessionToolAllowlist,
						customTools,
						sessionManager,
						settingsManager,
						resourceLoader
					})).session;
					session.setActiveToolsByName(sessionToolAllowlist);
					require_selection.applySystemPromptToSession(session, systemPromptText);
					await prepareCompactionSessionAgent({
						session,
						providerStreamFn,
						sessionId: params.sessionId,
						signal: runAbortController.signal,
						effectiveModel,
						resolvedApiKey: hasRuntimeAuthExchange ? void 0 : apiKeyInfo?.apiKey,
						authStorage,
						config: params.config,
						provider,
						modelId,
						thinkLevel,
						sessionAgentId,
						effectiveWorkspace,
						agentDir,
						runtimePlan,
						sessionKey: sandboxSessionKey,
						sandboxToolPolicy: sandbox?.tools,
						messageProvider: resolvedMessageProvider,
						agentAccountId: params.agentAccountId,
						groupId: params.groupId,
						groupChannel: params.groupChannel,
						groupSpace: params.groupSpace,
						spawnedBy: params.spawnedBy,
						senderId: params.senderId,
						senderName: params.senderName,
						senderUsername: params.senderUsername,
						senderE164: params.senderE164
					});
					session.agent.streamFn = require_attempt_model_diagnostic_events.wrapStreamFnWithDiagnosticModelCallEvents(session.agent.streamFn, {
						runId: diagnosticCompactionRunId,
						...params.sessionKey && { sessionKey: params.sessionKey },
						sessionId: params.sessionId,
						provider,
						model: modelId,
						api: effectiveModel.api,
						transport: session.agent.transport,
						contextTokenBudget,
						trace: compactionModelCallTrace,
						contentCapture: require_diagnostic_llm_content.resolveDiagnosticModelContentCapturePolicy(params.config),
						nextCallId: () => `${diagnosticCompactionRunId}:model:${diagnosticModelCallSeq += 1}`
					});
					const dedupedValidated = require_selection.dedupeDuplicateUserMessagesForCompaction(await require_selection.validateReplayTurns({
						messages: await require_selection.sanitizeSessionHistory({
							messages: session.messages,
							modelApi: effectiveModel.api,
							modelId,
							provider,
							allowedToolNames,
							config: params.config,
							workspaceDir: effectiveWorkspace,
							env: process.env,
							model: effectiveModel,
							sessionManager,
							sessionId: params.sessionId,
							policy: transcriptPolicy,
							preserveLatestAssistantThinking: false
						}),
						modelApi: effectiveModel.api,
						modelId,
						provider,
						config: params.config,
						workspaceDir: effectiveWorkspace,
						env: process.env,
						model: effectiveModel,
						sessionId: params.sessionId,
						policy: transcriptPolicy
					}));
					session.agent.state.messages = dedupedValidated;
					const originalMessages = session.messages.slice();
					const truncated = require_selection.limitHistoryTurns(session.messages, require_selection.getHistoryLimitFromSessionKey(params.sessionKey, params.config));
					const limited = transcriptPolicy.repairToolUseResultPairing ? require_session_transcript_repair.sanitizeToolUseResultPairing(truncated, {
						erroredAssistantResultPolicy: "drop",
						...effectiveModel.api === "openai-responses" || effectiveModel.api === "azure-openai-responses" || effectiveModel.api === "openai-chatgpt-responses" ? { missingToolResultText: "aborted" } : {}
					}) : truncated;
					if (limited.length > 0) session.agent.state.messages = limited;
					const hookRunner = require_build.asCompactionHookRunner(require_hook_runner_global.getGlobalHookRunner());
					const observedTokenCount = normalizeObservedTokenCount(params.currentTokenCount);
					const beforeHookMetrics = require_build.buildBeforeCompactionHookMetrics({
						originalMessages,
						currentMessages: session.messages,
						observedTokenCount,
						estimateTokensFn: require_session_manager.estimateTokens
					});
					const { hookSessionKey, missingSessionKey } = await require_build.runBeforeCompactionHooks({
						hookRunner,
						sessionId: params.sessionId,
						sessionKey: params.sessionKey,
						sessionAgentId,
						workspaceDir: effectiveWorkspace,
						messageProvider: resolvedMessageProvider,
						metrics: beforeHookMetrics,
						onHookMessages: params.onCompactionHookMessages
					});
					const { messageCountOriginal } = beforeHookMetrics;
					const diagEnabled = require_logger.log.isEnabled("debug");
					const preMetrics = diagEnabled ? summarizeCompactionMessages(session.messages) : void 0;
					if (diagEnabled && preMetrics) {
						require_logger.log.debug(`[compaction-diag] start runId=${runId} sessionKey=${params.sessionKey ?? params.sessionId} diagId=${diagId} trigger=${trigger} provider=${provider}/${modelId} attempt=${attempt} maxAttempts=${maxAttempts} pre.messages=${preMetrics.messages} pre.historyTextChars=${preMetrics.historyTextChars} pre.toolResultChars=${preMetrics.toolResultChars} pre.estTokens=${preMetrics.estTokens ?? "unknown"}`);
						require_logger.log.debug(`[compaction-diag] contributors diagId=${diagId} top=${JSON.stringify(preMetrics.contributors)}`);
					}
					if (!containsRealConversationMessages(session.messages)) {
						require_logger.log.info(`[compaction] skipping — no real conversation messages (sessionKey=${params.sessionKey ?? params.sessionId})`);
						return {
							ok: true,
							compacted: false,
							reason: "no real conversation messages"
						};
					}
					const compactStartedAt = Date.now();
					const messageCountCompactionInput = messageCountOriginal;
					let fullSessionTokensBefore = 0;
					try {
						fullSessionTokensBefore = limited.reduce((sum, msg) => sum + require_session_manager.estimateTokens(msg), 0);
					} catch {}
					const activeSession = session;
					const result = await require_diagnostic.compactWithSafetyTimeout(() => {
						require_selection.setCompactionSafeguardCancelReason(compactionSessionManager, void 0);
						return activeSession.compact(params.customInstructions);
					}, compactionTimeoutMs, {
						abortSignal: params.abortSignal,
						onCancel: () => {
							activeSession.abortCompaction();
						}
					});
					let effectiveFirstKeptEntryId = result.firstKeptEntryId;
					let postCompactionLeafId = typeof sessionManager.getLeafId === "function" ? sessionManager.getLeafId() ?? void 0 : void 0;
					let transcriptRotationSessionManager = sessionManager;
					if (params.trigger === "manual" && !isSqliteSessionTranscript) try {
						const hardenedBoundary = await hardenManualCompactionBoundary({
							sessionFile: params.sessionFile,
							preserveRecentTail: typeof params.config?.agents?.defaults?.compaction?.keepRecentTokens === "number"
						});
						if (hardenedBoundary.applied) {
							effectiveFirstKeptEntryId = hardenedBoundary.firstKeptEntryId ?? effectiveFirstKeptEntryId;
							postCompactionLeafId = hardenedBoundary.leafId ?? postCompactionLeafId;
							session.agent.state.messages = hardenedBoundary.messages;
							transcriptRotationSessionManager = await require_transcript_rewrite.readTranscriptFileState(params.sessionFile);
						}
					} catch (err) {
						require_logger.log.warn("[compaction] failed to harden manual compaction boundary", { errorMessage: require_errors.formatErrorMessage(err) });
					}
					const tokensAfter = require_build.estimateTokensAfterCompaction({
						messagesAfter: session.messages,
						observedTokenCount,
						fullSessionTokensBefore,
						estimateTokensFn: require_session_manager.estimateTokens
					});
					const messageCountAfter = session.messages.length;
					const compactedCount = Math.max(0, messageCountCompactionInput - messageCountAfter);
					let transcriptRotation = { rotated: false };
					if (require_selection.shouldRotateCompactionTranscript(params.config) && !isSqliteSessionTranscript) try {
						transcriptRotation = await require_selection.rotateTranscriptAfterCompaction({
							sessionManager: transcriptRotationSessionManager,
							sessionFile: params.sessionFile
						});
					} catch (err) {
						require_logger.log.warn("[compaction] post-compaction transcript rotation failed", {
							errorMessage: require_errors.formatErrorMessage(err),
							errorStack: err instanceof Error ? err.stack : void 0
						});
					}
					const activeSessionId = transcriptRotation.sessionId ?? params.sessionId;
					const activeSessionFile = transcriptRotation.sessionFile ?? params.sessionFile;
					const activePostLeafId = transcriptRotation.leafId ?? postCompactionLeafId;
					if (transcriptRotation.rotated) require_logger.log.info(`[compaction] rotated active transcript after compaction (sessionKey=${params.sessionKey ?? params.sessionId})`);
					await require_build.runPostCompactionSideEffects({
						config: params.config,
						sessionKey: params.sessionKey,
						sessionId: activeSessionId,
						agentId: sessionAgentId,
						sessionFile: activeSessionFile
					});
					if (params.config && params.sessionKey && checkpointSnapshot) try {
						const checkpointPosition = require_build.resolveCompactionCheckpointTranscriptPosition({
							preferredLeafId: activePostLeafId,
							transcriptState: await require_build.readSessionLeafStateFromTranscriptAsync(activeSessionFile)
						});
						checkpointSnapshotRetained = await compactionCheckpointStore.persistCheckpoint({
							cfg: params.config,
							sessionKey: params.sessionKey,
							sessionId: activeSessionId,
							reason: require_build.resolveSessionCompactionCheckpointReason({ trigger: params.trigger }),
							snapshot: checkpointSnapshot,
							summary: result.summary,
							firstKeptEntryId: effectiveFirstKeptEntryId,
							tokensBefore: observedTokenCount ?? result.tokensBefore,
							tokensAfter,
							postSessionFile: activeSessionFile,
							postLeafId: checkpointPosition.leafId,
							postEntryId: checkpointPosition.entryId,
							createdAt: compactStartedAt
						}) !== null;
					} catch (err) {
						require_logger.log.warn("failed to persist compaction checkpoint", { errorMessage: require_errors.formatErrorMessage(err) });
					}
					const postMetrics = diagEnabled ? summarizeCompactionMessages(session.messages) : void 0;
					if (diagEnabled && preMetrics && postMetrics) require_logger.log.debug(`[compaction-diag] end runId=${runId} sessionKey=${params.sessionKey ?? params.sessionId} diagId=${diagId} trigger=${trigger} provider=${provider}/${modelId} attempt=${attempt} maxAttempts=${maxAttempts} outcome=compacted reason=none durationMs=${Date.now() - compactStartedAt} retrying=false post.messages=${postMetrics.messages} post.historyTextChars=${postMetrics.historyTextChars} post.toolResultChars=${postMetrics.toolResultChars} post.estTokens=${postMetrics.estTokens ?? "unknown"} delta.messages=${postMetrics.messages - preMetrics.messages} delta.historyTextChars=${postMetrics.historyTextChars - preMetrics.historyTextChars} delta.toolResultChars=${postMetrics.toolResultChars - preMetrics.toolResultChars} delta.estTokens=${typeof preMetrics.estTokens === "number" && typeof postMetrics.estTokens === "number" ? postMetrics.estTokens - preMetrics.estTokens : "unknown"}`);
					await require_build.runAfterCompactionHooks({
						hookRunner,
						sessionId: activeSessionId,
						sessionAgentId,
						hookSessionKey,
						missingSessionKey,
						workspaceDir: effectiveWorkspace,
						messageProvider: resolvedMessageProvider,
						messageCountAfter,
						tokensAfter,
						compactedCount,
						sessionFile: activeSessionFile,
						...activeSessionId !== params.sessionId ? { previousSessionId: params.sessionId } : {},
						summaryLength: typeof result.summary === "string" ? result.summary.length : void 0,
						tokensBefore: result.tokensBefore,
						firstKeptEntryId: effectiveFirstKeptEntryId,
						onHookMessages: params.onCompactionHookMessages
					});
					return {
						ok: true,
						compacted: true,
						result: {
							summary: result.summary,
							firstKeptEntryId: effectiveFirstKeptEntryId,
							tokensBefore: observedTokenCount ?? result.tokensBefore,
							tokensAfter,
							details: result.details,
							sessionId: transcriptRotation.sessionId,
							sessionFile: transcriptRotation.sessionFile
						}
					};
				} catch (err) {
					const fallbackThinking = require_embedded_agent_helpers.pickFallbackThinkingLevel({
						message: require_errors.formatErrorMessage(err),
						attempted: attemptedThinking
					});
					if (fallbackThinking) {
						require_logger.log.warn(`[compaction] request rejected for ${provider}/${modelId}; retrying with ${fallbackThinking}`);
						thinkLevel = fallbackThinking;
						continue;
					}
					throw err;
				} finally {
					try {
						await require_selection.flushPendingToolResultsAfterIdle({
							agent: session?.agent,
							sessionManager
						});
					} catch {}
					try {
						session?.dispose();
					} catch {}
				}
			}
		} finally {
			try {
				await bundleMcpRuntime?.dispose();
			} catch {}
			try {
				await bundleLspRuntime?.dispose();
			} catch {}
			await sessionLock.release();
		}
	} catch (err) {
		return fail(require_compact_reasons.resolveCompactionFailureReason({
			reason: require_errors.formatErrorMessage(err),
			safeguardCancelReason: require_selection.consumeCompactionSafeguardCancelReason(compactionSessionManager)
		}), err);
	} finally {
		if (!checkpointSnapshotRetained) await compactionCheckpointStore.cleanupSnapshot(checkpointSnapshot);
	}
}
const testing = {
	hasRealConversationContent,
	hasMeaningfulConversationContent: require_selection.hasMeaningfulConversationContent,
	containsRealConversationMessages,
	estimateTokensAfterCompaction: require_build.estimateTokensAfterCompaction,
	buildBeforeCompactionHookMetrics: require_build.buildBeforeCompactionHookMetrics,
	hardenManualCompactionBoundary,
	resolveCompactionProviderStream,
	prepareCompactionSessionAgent,
	runBeforeCompactionHooks: require_build.runBeforeCompactionHooks,
	runAfterCompactionHooks: require_build.runAfterCompactionHooks,
	runPostCompactionSideEffects: require_build.runPostCompactionSideEffects
};
//#endregion
exports.compactEmbeddedAgentSessionDirect = compactEmbeddedAgentSessionDirect;
exports.testing = testing;
