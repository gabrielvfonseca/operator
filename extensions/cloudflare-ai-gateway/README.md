# Operator Cloudflare AI Gateway Provider

Official Operator provider plugin for Cloudflare AI Gateway.

Install from Operator:

```bash
operator plugins install @gabrielvfonseca/cloudflare-ai-gateway-provider
operator gateway restart
```

See <https://docs.operator.ai/providers/cloudflare-ai-gateway> for setup and configuration.
