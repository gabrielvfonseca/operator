# Venice Operator provider

Official Operator provider plugin for Venice.

## Install

```sh
operator plugins install @gabrielvfonseca/venice-provider
```

## Docs

See `docs/providers/venice.md` in the Operator repository, or the published docs at `https://docs.operator.ai/providers/venice`.
