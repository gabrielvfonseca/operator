interface:
  display_name: "Operator CI Limits"
  short_description: "Tune Operator CI fanout and runner budgets"
  default_prompt: "Use $operator-ci-limits to inspect Operator CI pressure, tune runner-registration fanout safely, and document the exact validation before landing."
