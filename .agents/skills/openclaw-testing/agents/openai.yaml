interface:
  display_name: "Operator Testing"
  short_description: "Choose cheap, targeted Operator validation"
  default_prompt: "Use $operator-testing to choose the cheapest safe test or CI verification path, inspect failures, and rerun only the relevant Operator lane."
