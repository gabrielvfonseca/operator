interface:
  display_name: "Operator Release CI"
  short_description: "Verify and debug Operator release validation runs"
  default_prompt: "Use $release-operator-ci to validate one frozen release SHA with one active parent run, recover only the smallest failed lane, and stop on a precise external blocker."
